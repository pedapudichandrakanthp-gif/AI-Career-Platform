# components

Reusable React components live here.

Use feature-focused subfolders as the platform grows, such as `ui`, `layout`, `jobs`, `profile`, `dashboard`, and `admin`. Keep components small, typed, accessible, and free of data-access or AI service logic.
