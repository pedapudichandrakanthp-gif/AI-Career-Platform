# types

Shared TypeScript types live here.

Use this folder for domain models and API contracts, including users, jobs, resumes, saved jobs, match scores, and common response shapes. Prefer precise interfaces and enums over loose types.
