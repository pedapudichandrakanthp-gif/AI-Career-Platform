# hooks

Reusable React hooks live here.

Use this folder for client-side state and browser interaction helpers that are shared across features. Hooks should be typed, focused, and avoid direct access to secrets or privileged server-only services.
